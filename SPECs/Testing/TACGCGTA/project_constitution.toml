# Project Constitution
# Generated by DNA Interview
# Date: 2025-11-02T05:52:00Z

[metadata]
dna_code = "TACGCGTA"
project_name = "spec-system"
constitution_version = "1.0"
generated_date = "2025-11-02T05:52:00Z"
system_constitution_version = "1.0"

[project]
purpose = "Creating a system for LLM-driven Spec-Driven Development with constitutional governance"
domain = "automation"
risk_level = "low"
complexity = "complex"
user_facing = false

[execution]
default_mode = "dynamic"
escalation_threshold = 3
confidence_threshold = 0.6

[quality]
critical_percentage = "20-40"
output_specificity = "moderate"
documentation_required = ["progress_log", "summary_report"]

[error_handling]
propagation_enabled = true
propagation_strategy = "continue_and_log"
failure_threshold = 5
read_prior_steps = true

[testing]
required = true
types = ["unit", "integration"]
tdd_preferred = true

[backups]
mandatory_for_critical = true
preferred_types = ["cognitive", "tool"]
custom_tools = []
max_backups_per_step = 2

[retry]
max_retries = 2
backoff_strategy = "exponential"

[resources]
time_limit_minutes = 0
task_timeout_minutes = 0
step_timeout_seconds = 0
external_dependencies = []
external_service_failures = "retry_with_backoff"

[constraints]
software_stack = "Markdown, TOML, LLM integration"
language = null
frameworks = []
tools = ["Markdown", "TOML", "JSON"]
prohibited_actions = [
    "Do not delete files outside project directory",
    "Do not modify system configuration",
    "Do not expose credentials in logs",
    "Do not install packages without approval"
]

[optional_features]
user_stories = "never"
clarification_phase = "complex_only"
research_enabled = false
quickstart_validation = true
parallel_execution = false

[logging]
detail_level = "standard"
include_metrics = true
retention_days = 90

[review]
required_before_execution = false
change_tracking = "git"

[compliance]
security_requirements = ["no_credentials_in_logs", "encrypt_sensitive_data"]
regulatory = []

[_inference_notes]
risk_reasoning = "LOW RISK: Internal developer tooling with no production deployment, user data, or critical infrastructure implications"
domain_reasoning = "Automation / Developer Tooling - meta-system for generating and executing specifications with LLM integration"
failure_strategy_reasoning = "continue_and_log appropriate for low-risk internal tool development"
user_stories_reasoning = "NOT user-facing - developer tool for spec generation, no user stories needed"
complexity_reasoning = "Complex - requires meta-system architecture, dual-file parsing (Markdown/TOML), constitutional validation, LLM orchestration, execution control"

[[version_history]]
version = "1.0"
date = "2025-11-02T05:52:00Z"
changes = "Initial constitution via DNA interview"

