# Project Constitution
# Generated by DNA Interview
# Date: 2025-11-02T05:51:00Z

[metadata]
dna_code = "GCTATCGA"
project_name = "secure-counselor-notes"
constitution_version = "1.0"
generated_date = "2025-11-02T05:51:00Z"
system_constitution_version = "1.0"

[project]
purpose = "Creating a secure notetaking app for counselors working with addiction-at-risk prisoners on probation"
domain = "software_dev"
risk_level = "high"
complexity = "complex"
user_facing = true

[execution]
default_mode = "collaborative"
escalation_threshold = 0
confidence_threshold = 0.6

[quality]
critical_percentage = "60-80"
output_specificity = "moderate"
documentation_required = ["progress_log", "summary_report"]

[error_handling]
propagation_enabled = true
propagation_strategy = "halt_on_critical"
failure_threshold = 2
read_prior_steps = true

[testing]
required = true
types = ["unit", "integration"]
tdd_preferred = true

[backups]
mandatory_for_critical = true
preferred_types = ["cognitive", "tool"]
custom_tools = []
max_backups_per_step = 2

[retry]
max_retries = 2
backoff_strategy = "exponential"

[resources]
time_limit_minutes = 0
task_timeout_minutes = 0
step_timeout_seconds = 0
external_dependencies = ["Encryption libraries", "Secure cloud storage", "Audit logging service"]
external_service_failures = "retry_with_backoff"

[constraints]
software_stack = "Python/TypeScript, encrypted database, secure hosting"
language = "Python"
frameworks = ["FastAPI", "React"]
tools = ["PostgreSQL", "encryption-at-rest"]
prohibited_actions = [
    "Do not delete files outside project directory",
    "Do not modify system configuration",
    "Do not expose credentials in logs",
    "Do not install packages without approval",
    "Do not store unencrypted sensitive data",
    "Do not bypass audit logging"
]

[optional_features]
user_stories = "user_facing_only"
clarification_phase = "complex_only"
research_enabled = false
quickstart_validation = true
parallel_execution = false

[logging]
detail_level = "standard"
include_metrics = true
retention_days = 90

[review]
required_before_execution = false
change_tracking = "git"

[compliance]
security_requirements = ["no_credentials_in_logs", "encrypt_sensitive_data", "audit_trail_required", "access_control_mandatory"]
regulatory = ["HIPAA-like", "data protection", "patient confidentiality"]

[_inference_notes]
risk_reasoning = "HIGH RISK: Sensitive patient data (addiction, criminal justice), vulnerable population, legal compliance requirements, potential for harm if breached"
domain_reasoning = "Software development - secure healthcare-adjacent application with strict compliance needs"
failure_strategy_reasoning = "halt_on_critical required - cannot afford failures in security, encryption, or access control"
user_stories_reasoning = "User-facing counselor application - user stories enabled for UX and workflow design"
complexity_reasoning = "Complex - requires end-to-end encryption, role-based access, audit trails, compliance frameworks, secure architecture"

[[version_history]]
version = "1.0"
date = "2025-11-02T05:51:00Z"
changes = "Initial constitution via DNA interview"

