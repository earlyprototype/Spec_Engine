import { Typography, Box } from '@mui/material';

export default function SettingsPage() {
  return (
    <Box>
      <Typography variant="h4">Settings</Typography>
      <Typography>Dashboard settings will appear here</Typography>
    </Box>
  );
}
